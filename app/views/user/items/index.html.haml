.row.justify-content-center.align-items-center
  .col-md-8.col-11
    %h1.fs-2.text-center.my-4 マイアイテム一覧
    .mb-2
      = link_to '追加', new_user_item_path, class: 'btn btn-primary'
    %ul.list-group
      - @items.each do |item|
        %li.list-group-item
          = link_to item.name, user_item_path(item)

